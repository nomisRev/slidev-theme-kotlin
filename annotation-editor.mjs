// Loaded by Node itself when a consuming deck's `vite.config.ts` imports
// `slidev-theme-kotlin/annotation-editor`, so this entry must be plain
// JavaScript: Vite's config bundler externalizes resolved node_modules
// imports, and Node refuses raw TypeScript under node_modules.
export { drawnAnnotationEditor } from './plugins/drawn-annotation-editor.mjs'
